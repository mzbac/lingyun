{
  "name": "@lingyun/agent-sdk",
  "version": "0.1.0",
  "type": "module",
  "description": "Headless LingYun agent runtime for Node.js (tools + skills + plugins).",
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true }); require('fs').rmSync('dist-test', { recursive: true, force: true })\"",
    "build": "pnpm run clean && pnpm --filter @lingyun/core build && node scripts/build.cjs",
    "prepack": "pnpm run build",
    "typecheck": "pnpm --filter @lingyun/core build && tsc -p tsconfig.json --noEmit",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "pnpm run build && pnpm run build:test && mocha --ui tdd ./dist-test/suite/*.test.js",
    "test:e2e": "pnpm run build && pnpm run build:test && mocha --ui tdd --timeout 300000 ./dist-test/e2e/*.test.js"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^2.0.2",
    "ai": "^6.0.6",
    "glob": "^13.0.0",
    "undici": "^7.18.2",
    "zod": "^4.1.8"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.11.16",
    "dts-bundle-generator": "^9.5.1",
    "esbuild": "^0.25.0",
    "mocha": "^11.7.5",
    "typescript": "^5.3.3"
  }
}
