{
  "name": "@kooka/core",
  "version": "0.2.2",
  "type": "module",
  "description": "Shared LingYun core library (runtime-agnostic agent primitives).",
  "repository": {
    "type": "git",
    "url": "https://github.com/mzbac/lingyun",
    "directory": "packages/core"
  },
  "license": "Apache-2.0",
  "main": "./dist/cjs/index.cjs",
  "types": "./dist/types/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "exports": {
    ".": {
      "types": "./dist/types/index.d.ts",
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.cjs"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\"",
    "build:types": "tsc -p tsconfig.json && node scripts/patch-dts.cjs",
    "build:esm": "esbuild src/index.ts --bundle --platform=node --target=node24 --format=esm --outfile=dist/esm/index.js",
    "build:cjs": "esbuild src/index.ts --bundle --platform=node --target=node24 --format=cjs --outfile=dist/cjs/index.cjs",
    "build": "pnpm run clean && pnpm run build:types && pnpm run build:esm && pnpm run build:cjs",
    "prepack": "pnpm run build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ai": "^6.0.86"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=24"
  }
}
