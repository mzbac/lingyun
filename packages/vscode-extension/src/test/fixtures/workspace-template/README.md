# LingYun Test Workspace

This is a deterministic fixture template for VS Code extension tests.

- `src/index.ts` provides a minimal TypeScript entrypoint.
- `src/sample.ts` contains stable symbols used by grep/LSP tests.
- `.vscode/agent-tools/` exists so workspace-tool discovery has a realistic layout.
